# Data

This directory intentionally has no hardcoded data files.

All locality, keyword, budget, and customer data is generated
dynamically by the AI agents at runtime based on user input.

No mock data. No demo data. No hardcoded cities. Everything is real.
